import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h1 className="text-3xl text-red-500">Mern Admin</h1>
    </div>
  );
}

export default App;
